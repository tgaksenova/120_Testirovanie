<Page x:Class="TRPO_120_Testirovanie.StudentsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TRPO_120_Testirovanie"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StudentsView">

    <Grid>
        <ListView x:Name="students" Margin="0,0,0,0">
            <ListView.View>
                <GridView>
                    <GridViewColumn>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock  FontSize="20" FontWeight="Bold" HorizontalAlignment="Left" Text="{Binding StudentID, StringFormat=Номер студенческого: {0}}"/>
                                    <TextBlock  FontSize="20" FontWeight="Bold" HorizontalAlignment="Left" Text="{Binding GroupNumber, StringFormat=Группа студента: {0}}"/>
                                    <TextBlock  FontSize="20" FontWeight="Bold" HorizontalAlignment="Left" Text="{Binding FirstName, StringFormat=Имя: {0}}"/>
                                    <TextBlock  FontSize="20" FontWeight="Bold" HorizontalAlignment="Left" Text="{Binding SecondName, StringFormat=Фамилия: {0}}"/>
                                    <TextBlock  FontSize="20" FontWeight="Bold" HorizontalAlignment="Left" Text="{Binding Patronymic, StringFormat=Отчество: {0}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
